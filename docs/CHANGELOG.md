## 1.8.4 — Agent 能力适配开发版（2026-09-16）

聊天思考档位、待处理请求兼容、连接与登录恢复、MCP 状态/授权入口、HEIC/HEIF/AVIF 识别与兼容预览、PNG 副本和缩略图；保留悬浮球防闪烁修订。

完整编译与原生运行验证尚未完成。独立图片模块 10 项检查通过；详见 RELEASE-1.8.4.md 和 validation/desktop-1.8.4-validation.json。

# 1.8.3 · UI 修订 3

- 修复任务中心空状态说明文字裁切，适配放大字号。
- 统一窗口顶部、右侧、底部的主题边距。
- 回复风格改为下拉选框，兼容服务器自定义风格，修正保存字段。
- Windows 与 macOS 使用同一份修复源码。

## 2026-09-15 · macOS 界面修订 2（版本仍为 1.8.3）

- 去掉独立软件标题栏和顶部居中的 Hermes 文字，内容区域延伸至窗口顶部。
- 保留系统原生红绿灯，整合到左侧导航顶部；收起侧栏时预留 80 pt 宽度，避免遮挡头像和内容。主内容标题区域仍可拖动窗口。
- 状态栏使用独立的单色耳机助手图标，透明底，18/36/54 px 多分辨率；启用 AppKit 模板着色，随系统菜单栏明暗与选中状态显示。
- 悬浮球右键菜单改用 macOS 原生菜单，系统处理点击桌面、切换应用和 Escape 取消；提问、截图、语音、打开主窗口和关闭悬浮球入口保留。

# 1.8.3 · macOS Apple Silicon 适配

在共用 1.8.3 修复之上，补齐 Mac 悬浮球、菜单栏入口、后台驻留、Dock 重新打开和应用菜单保存退出；支持 Control＋点击菜单、Command 图片缩放与截图授权检查。提供随包运行环境的本机离线生成流程。Mac 真机验证范围见 MACOS-VALIDATION.md。

# 1.8.3 · 2026-09-15

- 浮窗、聊天窗口和工作台附件名称可打开预览；只有独立的 × 按钮移除附件。
- 保留思考过程流式片段的原始空格、换行与缩进。
- 工作台“继续上次的事”最后一条会话不再显示底部分隔线。
- 桌面菜单及快速提问窗使用柔和阴影并留足透明边距。
- 截图覆盖层可获取键盘焦点，Esc 及右键均可取消。
- 兼容旧版 clarify.request / approval.request，以及新版 clarify / approval 服务端 JSON-RPC 请求。
- 支持多问题分别作答；主聊天、浮窗和任务中心显示同一待确认事项。
- 恢复会话中的 open_requests 与旧版 pending 字段；正确响应 request.cancel；保持运行中会话状态。

## 1.8.2

- 收窄导航栏的工作空间、账户、导航图标与展开按钮统一居中。
- 导航栏增加最多四条最近会话，置顶优先，按当前工作空间及项目筛选；收窄时通过时钟按钮打开同一列表。
- 桌面菜单增加仅在打开期间生效的 Windows 全局左键监听，点桌面、任务栏或其他程序可收起；原点击继续传给目标程序。
- 通过原生窗口命中检测处理高 DPI 坐标；菜单关闭即释放监听，取消尚未执行的关闭回调，避免影响重新打开的菜单。
- 安装程序、原生启动器与应用版本统一为 1.8.2。

## 1.8.1

- 界面修正版：左上角重组为 Hermes 品牌与账户/工作空间区域，保留导航收起和快捷入口。
- 托盘及悬浮球右键菜单改为统一的 Compose 菜单，使用内置中文字体，跟随主题、字号及减少动效设置；支持屏幕边缘避让和键盘操作。
- 悬浮球语音改为按住录音、松开识别并发送；停顿不提前结束，回复后不自动重开麦克风。
- 增加延迟会话取消、重复手势、目标会话隔离、忙碌排队、失败录音保留与重试验证。
- 防止上一条语音任务的完成/失败事件覆盖新的录音状态。
- 合并 1.8.0 启动修复；完整安装程序与源码统一为 1.8.1。

## 1.8.0+startup.1

- 补齐 Windows 运行环境缺失的 jdk.unsupported.desktop，修复启动即退出。
- 将 JVM 两个输出流合并记录，修复错误日志为空；加入启动模块验证。
- 原生启动器持续监控进程；新增小体积启动修复安装包。

# 1.8.0
- C1 图标、Office 兼容预览、HTML 降级渲染与模块启动修正。
- 关闭到托盘和后台任务。
- 悬浮球提问、会话切换、文件拖入、框选截图、连续语音与桌面助手设置。

# Hermes Windows 1.7.0 · 配置、阅读与附件

## 模型供应商

设置新增独立的“模型供应商”页面。可配置常用供应商 API Key，添加、编辑自定义 API 地址和模型，测试连接、读取模型，以及设为新对话的默认供应商。模型目录加载失败时，仍可进入供应商设置修正配置。

使用当前工作空间的网关配置接口；新网关使用自定义端点接口，旧网关仅在接口确实未提供时回退到配置合并。更新自定义端点时，API Key 留空会保留已有密钥，其他供应商及既有高级参数也会保留。密钥不会回显。测试已有端点需要临时输入密钥，直接保存已有配置无需重填。

## 聊天头像与文件

- 用户消息旁显示设置中的“我的头像”；未设置时显示姓名缩写。与 F1 应用图标、Hermes 助理头像分别管理。
- 文件选择器回到桌面事件队列后打开，显示正在读取和失败反馈。读取附件期间切换会话，文件仍归属于原来的草稿。
- 可将本地文件拖到聊天或工作台输入框，也可在资源管理器复制文件后按 Ctrl+V / Shift+Insert 粘贴。普通文字粘贴维持原有行为。
- 附件先显示为可移除条目，发送时才上传。较小文本作为文本内容，图片沿用图片附件通道，PDF、Office 等二进制原文件写入当前工作空间。
- 同一文件重试使用稳定路径，并核对服务器上已有文件的原始字节；不覆盖不同内容。失败会保留或恢复文字与附件，便于重试。
- 每次最多 10 个文件；图片最多 8 MB，其他文件最多 12 MB。不直接上传目录。

## HTML 预览

内嵌网页改为异步初始化，避免在 Compose/AWT 窗口创建阶段等待 JavaFX。加载中保留界面响应，加入超时、失败重试及“查看源码”入口。

HTML、DOM 数量和相对资源读取设有限额；每个资源请求有超时，图片和 CSS 读取也受总量约束。预览停用网页脚本、事件处理器和动画，外部链接由系统浏览器打开。这是静态网页阅读，不执行网页应用；结构过大时会提示并允许查看源码。

## Markdown 阅读与双栏

- 参考 Typora 主题的文档阅读方式，调整正文宽度、16 sp 字号、1.85 倍行高和段落留白。
- 重新梳理标题层级、引用、列表、代码片段、表格和分隔线；中文软换行不额外插入空格，英文软换行自然衔接。
- 双栏默认同步滚动，按 Markdown 内容块与实际源码换行位置映射。可以从任一侧滚动，也可随时切换“独立滚动”。
- 保留原有目录、查找替换、撤销重做、图片、列表续写、草稿、修改对比和保存冲突检查。

## 安装与验证

推荐退出旧版后运行 `Hermes-Windows-1.7.0-Setup.exe` 升级。便携更新包需整体替换 `app` 目录和 `Hermes.exe`，详见包内更新说明。F1 图标、当前主题和原有用户数据目录保持兼容。

自动化覆盖协议、配置保留、附件原始字节与失败恢复、头像组件、双向滚动和既有功能；另提供真实 Compose 组件渲染截图。当前验证环境为 Linux，Windows 原生选择器、文件拖放/剪贴板、JavaFX 内嵌窗口和安装运行还需 Windows 真机确认。未连接或更改你的真实网关。详细证据见 `WINDOWS-VALIDATION.md`。

实现参考：[Hermes 供应商配置](https://github.com/NousResearch/hermes-agent/blob/main/website/docs/integrations/providers.md)、[Hermes 配置与端点接口](https://github.com/NousResearch/hermes-agent/blob/main/hermes_cli/web_routers/config_env.py)、[Hermes 文件接口](https://github.com/NousResearch/hermes-agent/blob/main/hermes_cli/web_routers/files.py)、[JavaFX Swing 嵌入](https://openjfx.io/javadoc/11/javafx.swing/javafx/embed/swing/JFXPanel.html)、[Typora 默认主题](https://github.com/typora/typora-default-themes)、[Folio 主题](https://theme.typora.io/theme/Folio/)。Markdown 使用自身 Compose 排版实现，未复制这些主题的素材。


---

# Hermes Windows 1.6.0 · 桌面与文档体验

## 窗口与图标

- F1 清爽插画作为桌面应用图标，保留原始角色设计；应用图标与个人/助理头像独立。PNG 保留透明圆角，ICO 包含 16/20/24/32/40/48/64/96/128/256 像素。
- 拖动改为 AWT 屏幕坐标事件，避免移动窗口时局部坐标变化造成拖不动。完整双击只切换一次最大化，按钮区域不参与拖动。
- 最大化设置当前显示器工作区，并通过 WM_GETMINMAXINFO 设置原生边界，覆盖多显示器和任务栏位于不同边缘的情况。保留 Windows 11 DWM 圆角及 Windows 10 回退。

## 头像与交互

- 图片选择器绑定当前头像弹窗，支持直接点击头像选图；解码、裁剪、保存移到后台，增加处理中与错误反馈。支持 PNG/JPEG/GIF/BMP/WebP。
- 页面切换改为短位移、快速减速的淡入，减少突然闪空；悬停与按下反馈缩短起始延迟。仍遵循减少动效设置。

## Markdown

- 回车自动续写无序、有序、任务列表及引用；空列表回车退出；代码块与输入法组词不触发列表续写。
- 标题级别、删除线、格式再次点击取消、查找替换（可撤销）、文档内图片预览与相对链接/锚点。
- 后台解析、只绘制可见行号、光标跟随取消逐键滚动动画，减少长文档编辑抖动。保留源码、双栏、草稿、修改对比和保存前服务器冲突检查。

## 文件预览

- 图片：适应窗口、1:1、缩放、拖动平移、旋转与像素尺寸。
- PDF：单次打开文档、逐页绘制、跳页、缩略图、加密文件密码输入。
- Word（doc/docx）：段落、标题、强调、嵌入图片、表格阅读与缩放。
- Excel（xls/xlsx）：切换工作表、数字格式、已保存公式结果、合并单元格、每批 100 行/30 列与固定表头。
- PPT（ppt/pptx）：幻灯片绘制、缩略图、跳页和缩放。
- 网页（html/htm）：工作空间相对图片和 CSS 打包到内嵌浏览器，外部链接交给系统浏览器。

Office 预览不执行宏或外部公式，也不承诺复现全部 Office 字体、复杂排版、图表和动画。网页不运行脚本、不自动读取外部资源。

## 升级

新增 Apache POI 与更新后的 PDF 依赖。安装包自动更换应用依赖；便携更新包必须整体替换 app 目录。配置和草稿目录保持原有位置。

实现参考：[Windows 最大化边界](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-getminmaxinfo)、[Apache POI](https://poi.apache.org/)、[PDFBox](https://pdfbox.apache.org/download.html)、[jsoup](https://jsoup.org/download)。验证证据见 WINDOWS-VALIDATION.md。


---

# Hermes Windows 1.5.0 · 体验细化

- 窗口：双击改为识别完整点击，拖拽经过阈值才交给系统；Windows 11 使用 DWM 圆角，Windows 10 提供原生窗口形状回退，最大化恢复直角。记住普通窗口大小。
- 图标：清理应用图标的黑色不透明角，生成多尺寸 ICO；控件改用 Microsoft Fluent System Icons 的原版 20/24px Regular 与 Filled 资源，共 160 个。
- 菜单与弹窗：统一 10px／12px 圆角、紧凑行距、中性图标、分组间距、轻边框、悬停反馈和主按钮。工作空间、模型、文件、编辑、右键菜单采用同一套规则。
- 工作台：独立有限高度滚动容器和可见滚动条，小窗口和 150% 缩放可滚动到底部；增加输入框上方的滚轮回归。
- 主题：用真实界面缩略图替换抽象线框，并保持正确比例。
- Markdown：预览／编辑／双栏、语法着色、行号、搜索、目录、选区格式化、撤销重做、修改对比、阅读字号、任务清单、代码复制和草稿提示。窄窗口目录改用菜单。保留基础 Markdown 排版编辑入口。
- 中文编辑：输入法的一次组词作为一条撤销记录；编辑不经过 HTML 转换，原始文本保留。
- 动效：页面和提示淡入、悬停与按下渐变、开关滑动、折叠展开过渡；遵循减少动效设置。
- 分发：新增 NSIS 安装包和原生启动器，支持当前用户安装、开始菜单、可选桌面快捷方式和卸载保留用户数据。

这是客户端更新，继续使用现有 Hermes 网关与账户。安装包与便携版来自同一份已校验的 Windows 应用和运行环境。

验证记录及尚未在真机验证的项目见 WINDOWS-VALIDATION.md。


---

# 1.4.0

按飞书 PC 端参考重做分栏、窗口、设置及三套主题；重新绘制图标；模型读取失败采用非阻塞的明确状态与重试，识别服务器旧进程重启要求；改善图片错误与会话摘要展示。详见 [1.4.0 发布说明](RELEASE-1.4.0.md)。

# Hermes Windows 1.3.0 · 体验重构

这一版围绕“切换工作空间、开始对话、跟进执行、继续处理成果”重新组织界面，延续现有网关、项目和数据目录。

## 导航与视觉

- 工作空间与项目筛选合并到左侧导航，支持查找工作空间；收起导航后仍能切换。
- 模型选择移到聊天输入框，支持按模型或提供商搜索。切换中显示状态，完成前暂停发送；延迟返回不会把你带回上一段会话。
- 文件入口在刷新按钮旁，保留目录、近期产物、本机草稿、文档分栏和文件栏拖动调整。
- 重绘 52 个 SVG 图标，统一线条、尺寸和按钮状态。三套主题、深色模式、密度与字号选择继续保留。

## 输入与阅读

- 在“设置 → 对话与记忆”或输入框下方选择 Enter 发送 / Ctrl+Enter 发送。
- Enter 发送时，Shift+Enter 换行；Ctrl+Enter 发送时，Enter 换行。换行保留光标与选区逻辑，输入法组合输入时不触发发送。
- 修整完整发送点击区域，统一键盘和点击的提交入口。附件准备、模型切换、空内容、停止中等状态都有明确反馈。
- 已确认被服务器拒绝的消息恢复为草稿，可直接重试；未知提交结果先恢复状态，避免重复执行。
- 任务运行中可排队、补充指令或停止后发送。只有服务器确认停止后，才提交替代任务；队列可以查看、改写和移除。
- 已知的英文模型切换头转成简短中文记录；等待中断提示结构化显示。原始消息仍保留，引用和代码示例不被删除。
- 助理正文采用轻量阅读布局，执行活动默认折叠，可选择展开或只看回答；大纲可定位已加载的问题。
- 阅读旧消息时保留当前位置；切换会话保留本次运行期间的阅读位置。刷新最近消息时，保留能够与新结果衔接的已加载历史。

## 会话、成果与设置

- 侧边栏按置顶、今天、昨天和更早分组；完整会话页增加置顶、未读、执行中筛选，并提供清除筛选。
- 任务完成项连接原会话和已索引的文件，产物引用随本机完成记录保存。
- 文件根据类型显示图标；文件阅读、编辑、修改对比和未保存草稿流程继续保留。
- 本机输入与阅读选项可独立使用，服务器配置读取失败不会挡住这些设置。
- 提供商目录可以搜索、展开；Skills、工具集、MCP 采用统一状态与详情层次。MCP 已配置、已启用与工具数量分别呈现。
- 网关诊断分别检查 HTTP、登录、实时连接、会话读取和文件能力，显示各项结果并支持复制诊断。
- 保留 1.2.1 的会话分页修复：每页最多 100 条，空页正确结束，后续页失败保留已加载内容。

## 使用与升级

已有 Windows 1.2.0 / 1.2.1 便携版：关闭 Hermes，将旧的 `app/hermes-desktop.jar` 备份到 app 目录外，再用更新包同名文件替换。其余 66 个依赖逐字节核对一致。

首次使用或希望直接解压运行：使用完整 portable 包，完整解压后双击 `Start-Hermes.bat`。已包含 Windows x64 Java 21，无需自行安装 Java。

本机数据继续使用 `%LOCALAPPDATA%\HermesDesktop`，不需要清除登录状态或草稿。源码包包含源码、构建脚本、图标生成脚本、测试与验证记录。

## 验证范围

自动化测试 151 项：149 项通过，2 项 Windows DPAPI 原生测试因平台条件跳过。新增 9 项使用真实 Compose 组件、鼠标/键盘事件、控制器和本地 WebSocket 网关验证发送、重试、排队、停止和模型切换。

界面截图来自实际 Compose 渲染与演示数据，覆盖三种主题、深色、窄窗口、分栏与常见缩放比例。构建与测试在 Linux 完成，尚未在 Windows 真机或你的生产网关执行本包。语音、麦克风、通知、DPAPI 和 JavaFX 原生窗口仍需实际 Windows 环境确认。


---

# Hermes Windows 1.2.1 · 会话同步修复

- 修复返回空页但总数仍较大时，客户端抛出“服务器返回的会话列表不完整”并丢弃本轮结果。
- 每页仍限制为 100 条；空页结束分页，满页不再因偏小的总数提前结束。
- 普通与归档会话逐页显示；后续请求失败时保留已加载条目，完整同步成功后才替换旧列表。
- 保留真正的请求失败、响应格式错误与重复页防循环检查。错误提示去掉未经判断的“检查网关连接”；详情可以复制。
- 更新方式和验证记录见 `RELEASE-1.2.1.md`。

# Hermes Windows 1.2.0

这一版把工作台变成直接交办任务的入口，并修复 1.1.0 会话列表无法同步的问题。

## 这次最值得试的变化

- **直接开始任务**：在工作台输入目标、拖入或选择附件，点击“开始任务”。草稿按账号、工作空间和项目保存。模板会追加到已有文字后，不覆盖你的输入。
- **快速查找**：按 Ctrl K，输入关键词，用方向键选择、Enter 打开、Esc 返回。在当前工作空间查找操作、会话摘要和近期文件；“搜索会话内容”进一步搜索历史消息。
- **一眼找到入口**：宽窗口下显示带文字的导航和最近会话。Ctrl B 切换导航；聊天中按 Ctrl Shift F 收起辅助区域，切回后保留原来的文档和文件栏设置。
- **轻一些的界面**：去掉玻璃主题的厚重双边框，统一层次和留白。工作台同时展示需要确认的事项、执行中的任务、近期文件和最近对话。
- **更顺手的设置**：限制阅读宽度，开关整行可以点击，技能列表在宽度足够时使用两列。

## 修复与可靠性

会话请求限制为每页最多 100 条，解决网关拒绝 limit=200 导致首页和会话页报错的问题。普通会话和归档会话都支持分页；兼容无总数、短页和重叠页；异常响应不会再当作空工作空间。

同步失败、正在加载、真正没有数据分别显示。失败时保留已有会话并提供重试和原因入口。消息加载失败不再出现新会话欢迎语。搜索中不再短暂显示未筛选的全部列表，读取部分消息失败时会明确提示结果可能不完整。

新建会话防重复点击，附件准备完成后才允许发送。首页任务转入会话后先保存草稿再发送。旧消息读取结果不能覆盖刚启动的任务；快速切换工作空间和多次刷新时，较旧的会话同步结果不会覆盖较新的结果。

Windows 界面的主要快捷键统一显示 Ctrl。中文输入法组合输入期间，Enter 不会触发首页发送或快速查找确认。

## 升级

已经使用 Windows 1.1.0 便携版：使用 1.2.0 更新包，按包内说明替换 app/hermes-desktop.jar。它沿用 1.1.0 的其余依赖和 Java 运行环境。

首次使用或需要完整目录：完整解压 1.2.0 portable 包，双击 Start-Hermes.bat。不要只复制启动脚本；app 和 runtime 必须一起保留。

数据仍在当前 Windows 用户的 %LOCALAPPDATA%\HermesDesktop。升级不改变用户数据格式和目录。建议保留旧版目录，便于需要时回退。

## 验证范围

自动化回归使用模拟网关和演示数据，截图来自真实 Compose 组件渲染。没有连接你的生产网关执行任务，也没有在 Windows 真机上运行这一包。Windows 通知、语音、麦克风、DPAPI、窗口缩放和 JavaFX 原生窗口仍需要在实际机器上确认。详细结果见 WINDOWS-VALIDATION.md。

---

# 1.1.0 · 桌面体验更新

## 工作空间
- 64 dp 图标导航和单一设置入口；中央工作区与可收起、可拖动宽度的右侧文件栏。
- 首页重新组织继续对话、待确认状态和近期文件；哑光、纸感、玻璃有不同的阅读区、层次与布局。
- 聊天增加最近会话抽屉，保留当前上下文；完整会话列表分页读取，显示最新摘要。
- 统一弹窗、菜单、提示与控件；大字号和短窗口下固定弹窗操作区，正文滚动。

## 对话与文件
- 折叠同一轮连续助理消息，保留推理、文件、流式状态和原消息定位。
- 隐藏单独的 MEDIA 文件标记，代码示例和正文保留；内部上下文不进入会话摘要。
- 鼠标选中文字后右键引用；跳到底部与正文分离，长回复真正定位末尾，手动回看时不自动拉回。
- 文档和对话并排、可导航的文档目录、阅读缩放、表格横向滚动、修改差异查看。
- 文档保存延续写前版本核对、本机草稿恢复与另存；输入、切换和关闭前刷新编辑器内容。
- 后台整理历史产物，使用消息或会话时间，打开旧会话不再改变产物新旧顺序。
- 文件路径面包屑、名称／类型排序、隐藏文件选项、真实文件大小、读取错误与重试。

## 任务与设置
- 空任务状态合并，活动任务按确认／执行／完成组织；保留定时任务的服务器执行记录入口。
- 定时任务增加每天／工作日／每周时间选择、服务器时区和下三次执行预览。
- 定时任务保存与立即运行防重复提交；保存失败保留输入，不自动重发运行请求。
- 设置重新分组和搜索，高级模型选项默认折叠；Skills、工具集、MCP 可查找和筛选。
- 新增紧凑／标准密度、90%～130% 文字大小与发送方式；扩充英文界面标签。
- 设置草稿按账号和工作空间保留，加密存储；校验输入、处理服务器更新冲突，保存中继续输入不会被成功回调覆盖。
- 新建项目、会话重命名在请求成功后关闭，失败保留输入；详细说明与一般成功通知分开显示。

## 交付
- 预编译 Windows x64 便携包，含 Temurin Java 21 运行环境和 Windows 专用图形依赖。
- 沿用旧版本用户数据目录。保留 Windows 原生 EXE／MSI 与 macOS 源码构建入口。
- 检查结果和平台验证边界见 WINDOWS-VALIDATION.md。

---

# Beta 4 · Windows

- 增加 Windows x64 原生依赖、启动／演示／便携打包／MSI 入口与系统图标。
- 新增当前 Windows 用户 DPAPI 密钥保护，不采用 Linux 开发密钥回退。
- 适配 Ctrl 快捷键、原生文件选择、显示缩放下的初始窗口、语音和托盘通知。
- 保留 Beta 3a 布局及既有网关、恢复和文件功能。
- 本轮验证范围见 WINDOWS-VALIDATION.md。

# 更新记录

## 1.0.0-beta.3a

- 修复宽窗口下内容宽度约束失效，重整首页、任务中心与设置分区。
- 修复大卡片继承胶囊裁切，取消玻璃主题的全高浮动外框。
- 主题缩略图按统一逻辑坐标绘制，检查真实点击切换和 Retina 2 倍缩放。
- 任务按状态分列；审批和澄清详情使用统一弹窗。少量任务收紧高度，多任务分别滚动。
- 首页和历史摘要隐藏内部上下文标记及服务器附件路径。
- 文件入口根据当前页面返回聊天或文件区，保留聊天草稿和未保存文档。
- 本轮不改网关协议、认证逻辑、构建依赖及 Mac 启动／打包脚本。

## 1.0.0-beta.3

- 压缩桌面留白、列表和输入区，改为 60 dp 图标导航，合并账户与设置入口。
- 将文件列表移入可收起的右侧栏，目录浏览与聊天页面独立，文档在中部打开并保留聊天草稿。
- 去掉统一的矩形悬停层，为导航／列表／文字操作分别提供反馈和键盘焦点。
- 三套皮肤增加哑光分栏、纸张阅读卡片、渐变半透明悬浮面板等材质与布局差异。
- 跳到底部改为消息区外的独立操作区，统一所有应用弹窗及右键菜单。
- 新增文件栏状态、聊天草稿保留及富文本切换时序回归，保留现有登录与打包修复。
- 用户已确认 Beta 2 可以连接网关，基础功能可用。本版 UI 仍需在该 Mac 真机复核。

## 1.0.0-beta.2

- 阻止空用户名／密码提交；登录失败保留输入，增加密码显示／隐藏，连接期间禁止修改输入。
- 登录异常按网关、认证方式、密码提交、会话验证、本机存储、Profile 读取区分，提供中文说明及不含凭据的可复制诊断。
- 保持官方 JSON 登录协议及密码原文，不自动重试 401／429，也不绕过认证。
- 新增真实 HTTP 与加密 Cookie 的登录回归测试；保留前两次打包入口修订。
- macOS 安装包版本设为 1.0.1，应用内版本为 1.0.0-beta.2，便于识别新安装的客户端。
- 真实网关认证失败的根因仍待用户环境验证，本版不将诊断增强等同于已恢复连接。

## Beta 1 打包入口补丁 2 · 2026-09-10

- 修复将 `Package-macOS.command` 放入旧 Preview 1 工程后，因不存在 `packaging/prepare-native.sh` 而误报“放错目录”的问题。
- Preview 1 按原有源码打包；Beta 1 仍准备系统语音组件，组件文件缺失一部分时逐项报错并停止。
- 显示实际工程路径和源码版本，说明复制打包脚本不会将预览版升级为功能 Beta。

## Beta 1 打包入口补丁 · 2026-09-10

- 新增可单独复制到现有工程的 `Package-macOS.command`，执行原生组件准备、图标生成、主程序编译和 DMG 打包。
- 新入口不编译或运行自动测试，避免安装时因 Mockito 等测试依赖下载失败而中断；保留原 `Build-macOS.command` 的完整测试流程。
- 保存 Gradle 打包日志，打包失败时返回非零状态；没有更改应用源码、依赖版本、TLS 或证书校验。

## 1.0.0-beta.1

- 将预览框架扩展为完整的聊天、任务、文件和设置工作流。
- 增加按账号／Profile／会话隔离的加密工作状态、附件数据块、运行恢复、队列、录音和文档草稿。
- 在提交前保存任务；处理失败、断线和停止，避免自动重新提交不确定的任务。
- 增加连续语音面板、静音端点检测、噪声适应、分段朗读、系统回退、录音重试和中文简繁转换。
- 增加命令检索、提示词片段、会审入口和真实专家返回、推理与引用展示、搜索命中定位。
- 增加文件标签页、最近产物、恢复草稿、源码编辑、复杂 Markdown 保护和远端变化检查。
- 完善模型、记忆、审批、语音、通知、头像、语言、主题和网关更新设置。
- 增加菜单、窗口尺寸记忆、Dock 标记；JDK 检测兼容手动安装的 OpenJDK 21，无需 brew。
- 增加 macOS Speech 原生辅助程序和自动构建步骤；沿用原项目人物动画。

## 1.0.0-preview.1

独立 Compose Desktop 框架、四个已确认核心页面、第一轮网关适配与 Mac 启动／打包脚本。
