# POI/PDFBox log through the API's bundled lightweight provider.
# Keep diagnostic errors, without a misleading missing-provider warning at first preview.
log4j.provider=org.apache.logging.log4j.simple.internal.SimpleProvider
log4j2.simplelogLevel=WARN
